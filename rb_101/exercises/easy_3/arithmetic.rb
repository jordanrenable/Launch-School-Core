puts "==> Enter the first positive number:"
first = gets.to_f
puts "==> Enter the last positive number:"
last = gets.to_f

puts "==> #{first} + #{last} = #{first + last}"
puts "==> #{first} - #{last} = #{first - last}"
puts "==> #{first} * #{last} = #{first * last}"
puts "==> #{first} / #{last} = #{first / last}"
puts "==> #{first} % #{last} = #{first % last}"
puts "==> #{first} ^ #{last} = #{first**last}"
